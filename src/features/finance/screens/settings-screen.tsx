import { Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import type { SettingsViewModel } from '@/features/finance/view-models/use-settings';
import type { Translate } from '@/i18n/translations';
import { colors, spacing } from '@/theme';
export function SettingsScreen({ t, ...p }: SettingsViewModel & { t: Translate; onBack?: () => void; onOpenAccounts(): void; onOpenCategories(): void; onOpenSync(): void }) { return <ScrollView contentContainerStyle={styles.container}><Text style={styles.title}>{t('settingsTitle')}</Text><TextInput accessibilityLabel={t('settingsDisplayNameLabel')} placeholder={t('settingsDisplayNamePlaceholder')} value={p.displayName} onChangeText={p.setDisplayName} /><Pressable accessibilityLabel={t('settingsSave')} onPress={() => void p.saveDisplayName()}><Text>{t('settingsSave')}</Text></Pressable><Pressable accessibilityLabel={p.settings.amountsHidden ? t('settingsShowAmounts') : t('settingsHideAmounts')} onPress={() => void p.toggleAmountsHidden()}><Text>{p.settings.amountsHidden ? t('settingsShowAmounts') : t('settingsHideAmounts')}</Text></Pressable><Pressable onPress={p.onOpenAccounts}><Text>{t('settingsManageAccounts')}</Text></Pressable><Pressable onPress={p.onOpenCategories}><Text>{t('settingsManageCategories')}</Text></Pressable><Pressable onPress={p.onOpenSync}><Text>{t('settingsDataSync')}</Text></Pressable>{p.onBack ? <Pressable onPress={p.onBack}><Text>{t('settingsBack')}</Text></Pressable> : null}</ScrollView>; }
const styles = StyleSheet.create({ container: { backgroundColor: colors.surface.canvas, flexGrow: 1, gap: spacing[4], padding: spacing[4] }, title: { color: colors.content.primary, fontSize: 28, fontWeight: '700' } });
